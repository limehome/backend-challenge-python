services:
  api:
    build:
      context: .
    container_name: fastapi-application
    volumes:
      - ./app:/code/app
      - ./data:/code/data
    environment:
      PORT: 8000
    ports:
      - '8000:8000'
    restart: "no"